1. created one  file  and comitted , check the track

2.created  one folder, opened in vs

3. git config --global user.name "Shivaprasad-S-Devadiga"
   git config --global user.email "shivaprasada02@gmail.com"

4. copy the http link from the gihub repository Code(green)
    git clone https://github.com/Shivaprasad-S-Devadiga/devops.git

5.change tyhe directory(come inside the repository(devops))

6.checked using ls (files are there or not)

7. created  one file vs code(local) named prod.txt

finished
